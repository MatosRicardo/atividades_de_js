nome = "Ricardo"; // Não faça isso, pois gera uma variavel global
console.log(nome);

//Para redeclarar o Let

let number = 500;
number = 600; // sem escrever Let novamente, pois irá dar erro, diferente do "Var" que libera isso, mas foi mudado com (ES6)
